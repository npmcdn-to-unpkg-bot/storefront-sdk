class ContextActions {
  changeLocation(location) {
    return location;
  }

  setLoading(bool) {
    return bool;
  }

  replaceWithCache(URL) {
    return URL;
  }
}

export default ContextActions;
